# cmx — CleanMyMac X 命令行对标实现

```
cmx status               系统状态概览
cmx scan                 全模块智能扫描
cmx clean system-junk    清理系统垃圾（默认 dry-run）
cmx optimize [--apply]   性能优化
cmx uninstall <app>      卸载应用及残留
```

默认 **dry-run**（仅预览不删除），加 `--apply` 参数执行实际操作。

详见 `cmx help` 或上级目录 `SKILL.md`。
